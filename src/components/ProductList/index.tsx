const ProductList = () => {
  return (
    <>
      {/* {data?.map((product, index) => (
        <div key={index}>{product.name}</div>
      ))} */}
      <div>ProduactList</div>
    </>
  );
};

export default ProductList;
